{
  "scripts": {
    "start": "cd frontend && npm install && ng build --env=prod; cd ../backend && npm install && node server.js",
    "build": "cd frontend && npm install && ng build --env=prod; cd ../backend && npm install"
  }
}
